@model ChurchGivingRecorder.Models.Deposit

@{
    ViewData["Title"] = "Delete";
}

<form asp-action="Delete">
    <div class="modal-header">
        <h5 class="modal-title" id="createEditModalLabel">Delete Deposit</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <div class="modal-body">
        <input type="hidden" asp-for="Id" />
        <h3>Are you sure you want to delete this deposit?</h3>
        <div>
            <hr />
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.DepositDate)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.DepositDate)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Description)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Description)
                </dd>
            </dl>
        </div>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <input type="submit" value="Delete" class="btn btn-danger" />
    </div>
</form>

